import 'package:flutter/material.dart';

class SeekerHomePage extends StatelessWidget {
  static const String routeName = "/seeker";

  const SeekerHomePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Container(),
      ),
    );
  }
}
